<Window x:Class="SpokojnySklep.AddProductWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dodaj Produkt" Height="250" Width="300" ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,10">
            <Label Content="Nazwa:"/>
            <TextBox x:Name="ProductNameTextBox"/>
        </StackPanel>

        <StackPanel Margin="0,0,0,10" Grid.Row="1">
            <Label Content="Cena:"/>
            <TextBox x:Name="ProductPriceTextBox"/>
        </StackPanel>

        <StackPanel Margin="0,0,0,10" Grid.Row="2">
            <Label Content="Typ produktu:"/>
            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                <RadioButton x:Name="PhysicalRadio" Content="Fizyczny" IsChecked="True" Margin="0,0,10,0" Checked="PhysicalRadio_Checked"/>
                <RadioButton x:Name="DigitalRadio" Content="Cyfrowy"/>
            </StackPanel>
        </StackPanel>

        <Button Grid.Row="3" Content="Dodaj" Width="80" HorizontalAlignment="Center" Click="AddButton_Click"/>
    </Grid>
</Window>